name: PSX Scraper

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 1-5'      # 5:00 AM PKT (Mon-Fri)
    - cron: '0 3 * * 1-5'      # 8:00 AM PKT (Mon-Fri)
    - cron: '30 7 * * 1-5'     # 12:30 PM PKT (Mon-Fri)
    - cron: '0 10 * * 1-5'     # 3:00 PM PKT (Mon-Fri)

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests beautifulsoup4 openpyxl lxml html5lib

      - name: Verify installation
        run: |
          python -c "import pandas; print('Pandas:', pandas.__version__)"

      - name: Run scraper
        run: python main.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -A

          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi

          git commit -m "Update PSX data"

          git pull --rebase origin main

          git push
